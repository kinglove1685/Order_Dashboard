# Project Instructions (지침 참고용 파일)

## 목적
- 이 파일은 사용자가 제공하는 모든 지침을 누적 저장한다.
- 작업 시작 시 항상 이 파일을 먼저 읽고, 지침에 따라 결과를 만든다.
- 지침이 추가/변경되면 이 파일에 누락 없이 반영한다.

## 항상 적용 규칙
- 시작: 작업을 시작하기 전에 `AGENTS.md`를 읽는다.
- 누적: 사용자가 새 지침을 주면 **삭제 없이** 아래 "지침 로그"에 추가한다.
- 충돌: 기존 지침과 충돌하거나 모호하면 즉시 사용자에게 확인한다.
- 우선순위: 최신 지침이 과거 지침보다 우선한다(단, 충돌 여부는 확인).

## 작업 프로세스
1) `AGENTS.md` 읽기
2) 지침 요약 및 필요한 확인 질문(있을 때만)
3) 작업 수행(지침에 맞는 방식/출력)
4) 결과값 반환(파일 또는 데이터)
5) 새 지침이 있으면 "지침 로그"에 추가

## 결과값 반환 규칙
- 결과값은 **파일** 또는 **단순 데이터** 형태일 수 있다.
- 파일 결과: 생성/수정한 파일 경로를 명확히 제시한다.
- 데이터 결과: 핵심 값 위주로 간결하게 전달한다.
- 대용량 데이터는 요약하고 필요 시 파일로 제공한다.

## 보고서 규칙
- 보고서는 번호 소제목 형식으로 구성한다(예: 01. 수주현황, 02. ...).
- 01. 수주현황에는 월별 데이터를 표 형태로 넣는다.
- 02. 이후 소제목은 사용자의 지시에 따라 추가/갱신하며 누적해서 완전한 보고서를 만든다.
- 목표: 한 번의 지시로 전체 보고서를 생성할 수 있도록 구성과 내용을 유지한다.

## 지침 로그
- 2025-11-05: 사용자 요청으로 본 지침 파일 생성. 이후 지침은 이 섹션에 누적 기록한다.
- 2025-11-05: 사용자 지침은 모두 저장하고, 이후 유사 지침 시 그대로 따른다.
- 2025-11-05: 결과 파일은 한글이 깨지지 않도록 저장한다(예: UTF-8 BOM).
- 2025-11-05: 월별 데이터는 1DAY/FRP를 구분한 표 형태로 제공하고, 엑셀에는 서술형으로 쓰지 않는다.
- 2025-11-05: 보고서는 번호 소제목으로 구성하고 01. 수주현황에 표를 넣는다.
- 2025-11-05: 02. 이후 소제목은 사용자 지시에 따라 누적해 완전한 보고서를 만든다.
- 2025-11-05: 엑셀 열 이름은 사용자가 쓰는 한글 용어로 표기한다.
- 2025-11-05: Type(구분) 열은 맨 왼쪽에 두고 1-DAY/FRP로 구분한다.
- 2025-11-05: 엑셀 열 이름을 사용자 용어로 변경한다.
- 2025-11-05: 02 섹션에는 월/구분(1-DAY, FRP) 컬럼을 포함한다.
- 2025-11-05: 상태 기준은 생산완료예상일/포장실적을 사용하며, 작지번호가 여러 행일 때는 가장 늦은 상태를 사용한다.
- 2025-11-05: 작지번호가 여러 행일 경우 수치(오더수량/수주금액 등)는 합산한다.
- 2025-11-05: 작지번호가 여러 행일 경우 상태는 가장 덜 진행된(최소 진행) 상태로 판단한다.
- 2025-11-05: 생산완료예상일에 '체크필요'가 있으면 생산진행중으로 판단한다.
- 2025-11-05: 리드타임 기준일은 수주전송일과 포장완료일로 계산한다.
- 2025-11-05: 02 섹션에 포장완료일과 리드타임(일) 컬럼을 추가해 함께 본다.
- 2025-11-05: 월/구분별 요약 통계를 추가한다(작지건수, 리드타임 건수, 평균/최소/최대 리드타임).
- 2025-11-05: 엑셀 출력은 열 너비를 맞추고 숫자 형식은 #,###로 표시한다.
- 2025-11-05: 연도별 데이터는 시트명 25.xx, 26.xx만 포함해 집계한다.
- 2025-11-05: 월별 표는 1-DAY와 FRP 구분을 최우선으로 정렬한다.
- 2025-11-05: 포장완료일과 리드타임(일) 열은 맨 오른쪽에 배치한다.
- 2025-11-05: 02 표 맨 오른쪽에 납기준수(영업출고요청일), 납기준수(최초출고계획일) 두 열을 추가한다. 포장완료일이 없으면 포장완료예상일로 판단하며 둘 다 없으면 '포장계획수립필요'로 표기한다. 각 기준일이 없으면 해당 열에 '기준없음'으로 표기한다.
- 2025-11-05: summary_by_month 시트는 가장 오른쪽(마지막) 위치에 둔다.
- 2025-11-05: 데이터에 있는 5개 화폐의 원화 환율을 인터넷에서 5년치 일별로 수집해 엑셀로 제공한다(업무일 기준 환율은 결측일에 직전값으로 보간).
- 2025-11-05: report.md는 템플릿으로 유지하며 데이터 행 없이 형식만 보존한다. 기능 추가 시 해당 형식을 유지한 채 템플릿을 업데이트한다.
- 2025-11-05: 더블클릭 실행용 `run_generate_report.bat`과 자동 생성 스크립트 `generate_report.py`로 report.md를 지정 형식으로 자동 생성한다.
- 2025-11-05: 자동 생성 실행 로그를 `generate_report.log`에 남긴다.
- 2025-11-05: 수주금액(달러)은 화폐별 원화 환율을 사용해 포장완료일 기준으로 USD로 환산한다. 포장완료일이 없으면 포장완료예상일, 없으면 영업출고요청일을 사용하고 환율 날짜가 없으면 가장 가까운 날짜를 사용한다.
- 2026-01-04: 02 표에 화폐 컬럼을 추가하고 수주금액 옆에 배치한다.
- 2026-01-04: 수주금액(달러) 표시는 `$ #,###` 형식을 사용한다.
- 2026-01-04: 납기준수 두 열은 맨 오른쪽에 두고, 포장완료일/리드타임은 그 앞에 배치한다.
- 2026-01-04: 환율 데이터 갱신은 `update_fx_rates.py` 또는 `run_update_fx_rates.bat`로 수행한다.
- 2026-01-04: 최초출고계획일은 시트의 열 이름으로 식별해 사용하며, 해당 열이 없는 시트는 영업출고요청일을 대신 사용한다.
- 2026-01-04: 결과 엑셀에 `2026_monthly_summary.xlsx`의 `2026_summary` 시트 데이터를 동일하게 복사한 시트를 추가한다.
- 2026-01-04: 요약(MONTHLY SUMMARY) 시트는 `2025_2026_monthly_summary.xlsx` 데이터를 사용해 2025/2026을 함께 포함한다.
- 2026-01-04: 월별 요약(monthly_summary)은 항상 `order_status` 기준으로 재계산해 2025/2026을 모두 포함한다.
- 2026-01-04: 보고서 템플릿/결과(`report.md`)는 관련 결과 파일이 갱신될 때마다 자동으로 함께 갱신한다.
- 2026-01-04: Streamlit 대시보드를 만들어 `order_status`/`monthly_summary`/`summary_by_month` 3개 탭으로 제공하며, 메인 탭에서 `order_status` 전체 데이터를 필터/검색해서 볼 수 있게 한다.
- 2026-01-04: 대시보드 메인 요약은 연도별로 표시하며, 표시 컬럼 선택 UI는 제거하고 통합 검색 입력으로만 필터링한다.
- 2026-01-04: 대시보드 표시는 날짜에서 시간 제거, 모든 숫자는 `#,###` 형식으로 표시한다.
- 2026-01-04: 대시보드 표 정렬은 숫자 오른쪽, 날짜 가운데, 텍스트는 왼쪽 정렬로 통일한다.
- 2026-01-04: 대시보드의 연도별 요약은 통합 검색 결과에 맞춰 재계산해 표시한다.
- 2026-01-04: 대시보드 연도별 요약은 기본 표시이며, 버튼으로 월별 요약을 추가 표시한다.
- 2026-01-04: 대시보드 다운로드는 CSV와 엑셀 모두 제공하고, 한글 깨짐 이슈가 있으면 엑셀 다운로드를 사용한다.
- 2026-01-04: CSV/엑셀 다운로드는 화면의 숫자/날짜 형식을 유지한 값으로 저장한다.
- 2026-01-04: 대시보드에서 CSV 다운로드는 제거하고 엑셀 다운로드만 제공한다.
- 2026-01-04: 대시보드 디자인은 가독성과 심미성을 높이는 스타일(폰트/색상/레이아웃)을 적용한다.
- 2026-01-04: 상세 리스트는 기본적으로 올해 데이터만 보여주고, 사이드바에서 연도 기간을 설정할 수 있게 한다. 연도별 요약은 모든 연도를 표시한다.
- 2026-01-04: 연도 기간 설정 UI는 통합검색 입력 바로 위에 배치한다.
- 2026-01-04: 현재상태 컬럼은 푸른 계열 색으로 단계별(출고완료가 가장 진하게, 이전 단계일수록 연하게) 강조 표시한다.
- 2026-01-05: 상태 컬럼만 색상 적용하고 나머지 정렬/표시 포맷은 유지한다(속도 절충).
- 2026-01-05: 상세 표 행이 많으면 속도를 위해 스타일을 최소화하고, 검색/필터로 줄이면 상태 색상을 적용한다.
- 2026-01-05: 메인 탭 기간 설정은 연/월 범위로 선택하며, 기본 표시 범위는 현재 월부터 2개월 이후까지로 설정한다.
- 2026-01-05: 메인 탭 기간 설정은 날짜 범위 선택으로 제공하고, 기본 표시 범위는 현재 월부터 2개월 이후까지로 설정한다.
- 2026-01-05: 생산완료예상일은 시간 없이 날짜만 표시한다.
- 2026-01-05: 년도별 요약/월별 요약 표는 데이터 행 수에 맞춰 높이를 자동으로 줄여 빈 행이 보이지 않게 한다.
- 2026-01-05: 기간 선택 옆에 당월/당분기/당반기/올해/작년 빠른 선택 버튼을 제공한다.
- 2026-01-05: 기간 빠른 선택 버튼은 연한 회색 톤으로 통일하고, 오른쪽 정렬로 배치하며 올해 기준 2개년/3개년 버튼을 추가한다.
- 2026-01-05: 기간 빠른 선택 버튼의 글씨/버튼 크기는 작게, 라벨은 줄바꿈 없이 가로로 표시한다.
- 2026-01-05: 기간 빠른 선택 버튼 간격은 더 촘촘하게 하고 오른쪽에 붙여 배치한다.
- 2026-01-05: 기간 빠른 선택 버튼 간격이 균일해 보이도록 버튼 폭을 열 너비에 맞춘다.
- 2026-01-05: 상세 표 행이 많을 때는 현재상태 색상만 생략하고 나머지 숫자/날짜 서식은 유지한다.
- 2026-01-05: 년도별 요약은 구분(1-DAY/FRP)까지 포함해 분리 집계한다.
- 2026-01-05: 년도별 요약에는 구분별 합계 외에 연도 합계(구분=합계)도 함께 표시한다.
- 2026-01-05: 년도별 요약에서 연도 셀은 중복 표시를 숨겨 병합된 것처럼 보이게 하고, 합계 행은 굵게 표시한다.
- 2026-01-05: 월별 요약에서도 연도 셀은 중복 표시를 숨겨 병합된 것처럼 보이게 한다.
- 2026-01-05: 월별 요약은 구분(1-DAY/FRP)까지 포함해 분리 집계한다.
- 2026-01-05: 월별 요약에는 구분별 합계 외에 월 합계(구분=합계)도 함께 표시하고, 합계 행은 굵게 표시한다.
- 2026-01-05: 결과 엑셀에 작지번호+품명 기준으로 집계한 `order_status_by_item` 시트를 추가한다.
- 2026-01-05: 스트림릿 탭은 `수주 진행 상세`와 `제품별 수주 진행`만 유지하고, 제품별 탭은 `order_status_by_item` 시트를 동일한 형식으로 표시한다.
- 2026-01-05: 상태 색상 중 생산진행중은 흰색 배경으로 표시한다.
- 2026-01-05: 년도별/월별 요약에 오더수량(합계) 컬럼을 추가한다.
- 2026-01-05: 통합검색은 쉼표(,)로 구분된 값은 OR로, 공백으로 구분된 값은 AND로 검색한다.
- 2026-01-05: 제품별 탭은 사이드바 월 필터를 공유하지 않고 기간 선택(날짜 범위) 기준으로만 월을 필터링한다.
- 2026-01-05: 기간 빠른 선택 버튼에 최대기간(전체기간) 버튼을 추가한다.
- 2026-01-05: 현재상태 열은 가운데 정렬한다.
- 2026-01-05: 스트림릿 사용 메뉴얼을 마크다운과 워드(docx)로 제공한다.
- 2026-01-05: 생산완료 상태면 생산완료예상일이 비어있을 경우 해당 칸에 '생산완료'를 표시한다.
- 2026-01-05: 생산완료예상일 값이 '종이라벨/종이박스/클리어렌즈 비용/컬러렌즈 비용'인 행은 결과 생성에서 제외한다.
- 2026-01-05: 생산완료예상일은 날짜와 텍스트가 혼합되므로 스트림릿에서는 날짜는 날짜로 표시하고 텍스트(예: 생산완료)는 그대로 표시한다.
- 2026-01-05: 스트림릿 엑셀 다운로드에서 생산완료예상일은 시간 없이 날짜 문자열로 저장한다.
- 2026-01-05: 엑셀 결과 생성은 AI 없이 실행파일로도 구동되도록 제공한다.
- 2026-01-05: 대시보드 UI 요소 간격은 서로 겹치지 않도록 약간의 여백을 둔다.
- 2026-01-05: 데이터프레임 간 간격은 더 넉넉하게 띄워 겹침 없이 보이게 한다.
- 2026-01-05: 요소 간 간격은 내부 요소가 아닌 `stElementContainer` 기준으로 여백을 줘 겹침을 방지한다.
- 2026-01-06: 결과 엑셀 생성 시 포장완료예상일 오른쪽에 `포장 진도율` 컬럼을 추가하고, 포장실적 합계를 작업의뢰서 기준(제품별 시트는 작지+품명 기준)으로 오더수량 합계로 나눈 뒤 %로 표시한다.
- 2026-01-06: 기간 빠른 선택의 2개년/3개년은 미래가 아니라 과거 기준(현재 포함)으로 설정한다.
- 2026-01-06: 원본의 `생산/포장 특이사항`을 집계해 결과 파일에 추가하고, 같은 작업의뢰서에 여러 항목이 있으면 텍스트를 합쳐 표시한다.
- 2026-01-06: 생성된 엑셀에서는 `생산/포장 특이사항`을 가장 오른쪽 열로 배치하고, 스트림릿에서는 `연도` 열의 왼쪽에 배치한다.
- 2026-01-06: 스트림릿에 생산 이슈 관리 탭을 추가해 작지번호/거래처/제품/특이사항/해결여부/종결일/안건상정일을 리스트로 관리하고, 미해결과 종결을 구분해 확인한다. 해결여부는 전체 선택 버튼으로 일괄 체크할 수 있게 한다.
- 2026-01-06: 생산 이슈 관리에서 해결여부를 체크하고 저장하면 종결일을 저장 당일로 자동 입력한다.
- 2026-01-06: 환율 리스트 업데이트 기능은 실행파일(EXE)로도 제공한다.
- 2026-01-08: 스트림릿 실행용 EXE(대시보드 런처)를 제공한다.
- 2026-01-08: 스트림릿 실행 파일을 실행하는 BAT(`run_streamlit_dashboard.bat`)를 제공한다.
- 2026-01-08: 리드타임이 200일 이상이거나 음수면 수주접수일/포장완료일 연도를 ±1년 자동 보정해 리드타임을 계산하고, 보정 내역은 `리드타임 검토`에 표시한다. 보정으로도 유효값이 없으면 검토 대상으로 남긴다.
- 2026-01-08: 포장완료일이 미래일로 들어온 경우에는 포장완료일을 -1년 보정해 리드타임을 계산한다.
- 2026-01-08: 결과 파일명에서 연도 표기를 제거해 `order_status_with_leadtime.xlsx`로 사용한다.
- 2026-01-08: 통합검색에서 쉼표가 회사명 접미사(Inc, Ltd, Co., Ltd 등)에 포함된 경우는 분리하지 않고 한 덩어리로 검색한다.
- 2026-01-08: 스트림릿 엑셀 다운로드는 합계 행(데이터 위)과 굵은 헤더, 숫자/날짜 서식, 자동 열 너비를 포함해 제공한다.
- 2026-01-08: 스트림릿 엑셀 다운로드는 눈금선을 끄고(그리드라인 숨김), 셀 테두리 라인을 적용하며 열 너비를 텍스트가 보이도록 맞춘다.
- 2026-01-08: 스트림릿 엑셀 다운로드는 1행(합계행)에 테두리 라인을 적용하지 않는다.
- 2026-01-08: 년도별/월별 요약에 납기준수율(최초출고계획일)을 추가하며, 총 작지건수에서 '지연' 건을 뺀 비율(%)로 계산한다.
- 2026-01-08: 납기준수율 계산 시 지연 건이 없으면 100%로 표시한다(지연 0건 처리).
- 2026-01-08: 생산 이슈 관리 탭은 각 행 높이를 3배로 늘리고, 테이블 전체 높이는 기존 수준으로 유지한다.
- 2026-01-08: 년도별/월별 요약 테이블의 열 너비를 헤더 길이에 맞게 조정한다.
- 2026-01-08: 납기준수(영업출고요청일)에서 지연은 `CAPA부족 지연`으로 표시한다(엑셀/스트림릿 모두).
- 2026-01-08: 대시보드를 쉽게 실행할 수 있도록 `대시보드 실행.bat`을 제공한다.
- 2026-01-08: `대시보드 실행.bat`은 새 콘솔에서 대시보드를 실행하고 `http://localhost:8501`을 자동으로 연다.
- 2026-01-09: `run_streamlit_dashboard.bat`와 `streamlit_dashboard.exe`는 사용 중단으로 삭제(사용자 요청)하며, 대시보드 실행은 `대시보드 실행.bat`만 유지한다.
- 2026-01-09: `order_status_builder` 실행파일은 onedir 빌드(`dist/order_status_builder/`)도 제공한다.
- 2026-01-09: 스트림릿 배포용 파일은 GitHub `https://github.com/ssungjun83/Order_Dashboard` 저장소에 업로드한다.
- 2026-01-09: 납기준수(영업출고요청일) 지연 표기는 `지연`으로 유지하고 `CAPA부족 지연` 치환은 사용하지 않는다.
- 2026-01-09: 생산완료예상일은 동일 작지번호(또는 작지+품명) 그룹에서 날짜값이 있으면 가장 늦은 날짜를 우선 사용하고, 날짜가 없을 때만 기존 텍스트 상태(예: 생산완료)를 유지한다.
- 2026-01-09: 생산완료예상일에 `M/D` 형식 텍스트가 있으면 기준일(수주전송일→영업출고요청일→최초출고계획일→포장완료일→월 문자열)로 연도를 보정해 날짜로 변환 후 가장 늦은 날짜를 선택한다.
- 2026-01-09: 생산완료예상일에 날짜(또는 날짜 형식 텍스트)가 있으면 현재상태는 생산진행중으로 판단한다.
- 2026-01-09: 엑셀 생성은 EXE 대신 `generate_order_status.bat`로 실행할 수 있게 제공한다.
- 2026-01-09: 스트림릿에 `제품 수요 요약` 탭을 추가해 기간/검색 범위 내 품명별 평균수요, PO횟수, 연속 PO 횟수, 점유율을 산출해 표시한다.
- 2026-01-09: 제품 수요 요약 탭에 `총 오더수량` 컬럼을 추가한다.
- 2026-01-09: 스트림릿에 `제품 월별 수주` 탭을 추가해 월별 수주(1~12월), 총 수주, 연속수주, 가중점수, 평균수요를 표시한다(기간/검색/거래처 필터 반영).
- 2026-01-09: 제품 월별 수주 탭은 연도별로 행을 구분해(연도 컬럼 추가) 월별 트렌드를 연도 단위로 확인할 수 있게 한다.
- 2026-01-09: 제품 월별 수주 탭은 선택 기간 기준으로 `YYYY-MM` 월 컬럼을 동적으로 생성해(2개년이면 24개월) 표시하고, 가중점수는 선택 기간의 최근 6개월 합계로 계산한다.
- 2026-01-09: 제품 월별 수주 탭은 기간이 연도를 넘는 경우 시작/끝 연도를 전체 월(1~12월)로 확장해 2개년이면 24개월, 3개년이면 36개월을 항상 표시한다.
- 2026-01-09: 제품 월별 수주 탭은 품명 왼쪽에 거래처 컬럼을 추가해 함께 표시한다.
- 2026-01-09: 사용자용 메뉴얼(마크다운/워드)을 최신 탭 구성과 계산 규칙 기준으로 보강한다.
- 2026-01-09: 통합검색 동작(OR/AND 규칙, 검색 범위, 예외, 예시)을 메뉴얼에 상세히 설명한다.
- 2026-01-14: 포장단위는 숫자는 소수점 없는 `#,###` 형식으로, 텍스트는 그대로 표시한다.
- 2026-01-14: 제품별 수주 진행의 포장단위 열은 오더수량 오른쪽에 배치한다.
- 2026-01-15: 생산 이슈 관리 탭에도 수주 진행 상세와 동일한 기간 설정 UI를 추가한다.
- 2026-01-15: 수주 대시보드의 기본 파일 표시(기본 파일: ...) 캡션을 숨긴다.
- 2026-01-17: 납기준수(최초출고계획일) 표기를 납기준수(영업협의출고일)로 변경한다.
- 2026-01-17: 거래처별 금액 화면에 엑셀 다운로드 기능을 추가한다.

