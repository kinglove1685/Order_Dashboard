# 해외수주파일 요약 사용 메뉴얼

## 1. 목적
이 메뉴얼은 **스트림릿 화면에서의 사용법**만 설명합니다.

## 2. 화면 구성
탭은 5개로 구성됩니다.
- **수주 진행 상세**: 작업의뢰서(작지번호) 기준 집계
- **제품별 수주 진행**: 작업의뢰서 + 품명 기준 집계
- **제품 수요 요약**: 품명 기준 우선순위/수요 지표 요약
- **제품 월별 수주**: 기간 범위의 연-월(YYYY-MM) 기준 수요 테이블
- **생산 이슈 관리**: 생산/포장 특이사항 관리

## 3. 파일 업로드
- 좌측 사이드바의 **엑셀 업로드**에서 파일을 올리면 즉시 화면에 반영됩니다.
- 업로드가 없으면 기본 파일이 자동으로 표시됩니다.

## 4. 기간 선택
- 기간은 날짜 범위로 선택합니다.
- 빠른 선택 버튼으로 즉시 적용할 수 있습니다.
  - 당월 / 당분기 / 당반기 / 올해 / 작년 / 2개년 / 3개년 / 최대기간
- **제품 월별 수주** 탭은 기간이 연도를 넘으면 해당 연도의 1~12월을 모두 표시합니다.

## 5. 필터 및 검색
### 5.1 사이드바 필터
- **수주 진행 상세** 탭에서만 사용합니다.
- 월, 구분, 국가, 담당자, 고객을 복수 선택할 수 있습니다.
- 수주 진행 상세 탭에서 선택한 필터는 다른 탭에도 동일하게 적용됩니다.

### 5.2 통합 검색 규칙
#### 5.2.1 기본 동작
- 통합 검색은 **모든 열을 대상으로 부분 일치** 검색합니다.
- 대소문자를 구분하지 않습니다.
- 숫자/날짜도 문자열로 변환되어 검색됩니다.

#### 5.2.2 구분자 규칙 (OR/AND)
- 쉼표(,)로 구분된 값은 **OR 검색**
  - 예: `DD0174, DD0175` → 둘 중 하나라도 포함된 행 출력
- 공백으로 구분된 값은 **AND 검색**
  - 예: `PIA 1-DAY` → 두 단어가 모두 포함된 행 출력
- 쉼표와 공백을 함께 쓰면 **쉼표 그룹은 OR**, 각 그룹 안의 공백은 **AND**로 처리됩니다.
  - 예: `PIA 1-DAY, FRP 25.01`  
    → `(PIA AND 1-DAY) OR (FRP AND 25.01)`

#### 5.2.3 회사명 쉼표 예외
- 회사명 접미사(Inc, Ltd, Co., Ltd 등)에 포함된 쉼표는 **검색 분리하지 않습니다.**
  - 예: `ACME, Inc.` → 한 덩어리로 검색

#### 5.2.4 탭별 검색 범위
- 모든 탭의 통합검색은 **해당 탭에서 보여주는 데이터 전체 열**을 대상으로 합니다.
  - 예: 제품 월별 수주 탭에서 `sincere` 입력 → 거래처/품명/국가 등 모든 열 중 포함된 행 출력

#### 5.2.5 실전 예시
- 특정 작지번호 여러 개: `DD0174, DD0175, DD0177`
- 품명 + 구분 동시: `Dazzle 1-DAY`
- 거래처 + 국가 동시: `PIA 일본`

#### 5.2.6 주의사항
- 특수문자(예: `+`, `(`, `)`, `.`)가 포함된 검색은 일부 환경에서 다르게 동작할 수 있습니다.
- 원하는 결과가 너무 많으면 **검색어를 더 구체적으로** 입력하세요.

## 6. 요약 표
### 6.1 연도별 요약
- 구분(1-DAY/FRP)별 집계 + 합계 행 표시
- 연도 중복 표시는 숨겨 병합처럼 보이게 처리
- 합계 행은 굵게 표시
- **오더수량 합계** 포함
- **납기준수율(영업협의출고일)** 포함

### 6.2 월별 요약
- 연도 + 월 + 구분 기준 집계 + 월 합계 행 표시
- 연도 중복 표시는 숨겨 병합처럼 보이게 처리
- 합계 행은 굵게 표시
- **오더수량 합계** 포함
- **납기준수율(영업협의출고일)** 포함

## 7. 탭별 상세
### 7.1 수주 진행 상세
- 수주/리드타임 요약과 상세 리스트를 제공합니다.
- 현재상태는 단계별 색상으로 표시됩니다.

### 7.2 제품별 수주 진행
- 품명까지 포함한 상세 리스트를 제공합니다.

### 7.3 제품 수요 요약
- 품명별 지표를 요약해 보여줍니다.
- 지표: **우선순위, 평균수요, 총 오더수량, PO횟수, 연속 PO 횟수, 점유율**

### 7.4 제품 월별 수주
- 컬럼은 **YYYY-MM** 형식으로 표시됩니다.
- 품명 왼쪽에 **거래처**가 함께 표시됩니다.
- 지표 계산 방식
  - **총 수주**: 월별 수량이 0보다 큰 달의 개수
  - **연속수주**: 월 기준 연속 수주 최대 길이
  - **가중점수**: `(총 수주/1000) + (연속수주*5) + (선택 기간 최근 6개월 합계*2/1000)`
  - **평균수요**: `총수량 / 연속수주` (연속수주가 0이면 0)

### 7.5 생산 이슈 관리
- 미해결/종결 리스트가 분리되어 표시됩니다.
- **해결여부** 체크 후 저장하면 **종결일이 자동 입력**됩니다.
- **안건상정일**은 직접 입력할 수 있습니다.
- **전체 해결** 버튼으로 미해결 항목을 일괄 체크할 수 있습니다.
- 긴 메모를 입력할 수 있도록 행 높이를 넓게 표시합니다.

## 8. 표 표시 및 서식
- 숫자: `#,###` 형식
- 날짜: `YYYY-MM-DD` 형식 (시간 제거)
- 텍스트 정렬: 왼쪽
- 숫자 정렬: 오른쪽
- 날짜 정렬: 가운데
- 현재상태 정렬: 가운데
- 현재상태 색상은 단계별 파랑 계열, **생산진행중은 흰색**

## 9. 다운로드
- **수주 진행 상세**, **제품별 수주 진행** 탭에서 엑셀 다운로드가 제공됩니다.
- 다운로드 파일은 **합계 행 + 헤더 굵게 + 서식 유지 + 열 너비 자동**이 적용됩니다.
- 눈금선은 숨기고 셀 테두리 라인을 적용합니다(합계 행은 테두리 없음).

## 10. 참고 사항
- 행이 많으면 성능을 위해 **현재상태 색상만 생략**됩니다.
- 검색/필터로 범위를 줄이면 색상이 다시 표시됩니다.
